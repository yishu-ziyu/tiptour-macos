# 能听见你并跑出失败清单：地图（2026-09-25）

现行工作契约。接在 [产品定义地图](2026-09-25-product-definition-map.md) 之后，方法相同：只有一个目的地；决定、停放的线和收件箱只记在这里。

## 目的地

Her 能正常听你说话、回你的话；然后用当前构建把 [验收任务](../PRODUCT.md#验收任务) 1–9 各跑至少一次（10 隔天跑），得到第一版失败清单：每次运行用户判通过或不通过，并用自己的话写下哪里不对，再归成几类失败。

状态：**停放**（2026-09-25，用户决定语音暂时停掉，下一个目的地改为 [把活交给 Claude Code](2026-09-25-claude-code-delegation-map.md)）。本页停在下表所记的状态，恢复时从步骤 1d 接着走。

## 步骤与验收

| # | 步骤 | 怎样算做到（可证伪） | 状态 |
| --- | --- | --- | --- |
| 1 | 修复麦克风被系统停掉后不再录音 | 用户在 Xcode 构建后连续开 3 次会话，每次说「你好」都听到她回话；调试记录里每次都有 `user_turn_started` 和 `output_transcript_final`；凡出现 `audio_configuration_changed`，其后必须紧跟 `microphone_restarted`，或者用户看到停止提示。任何一次「看起来在听却没反应」都算失败 | 第二版在真实构建上：07:38、07:45 两次会话「你好」都有回话（`mode: same_engine`）。还差 1 次 |
| 1b | 连续对话时回话被中断 | 同一会话里连说 3 句、每句等她说完再说：3 句都听到完整回话；插话时她停下并回应新的一句，之后不卡住 | 07:39 会话复现一次（第 2、3 句字幕出现但声音中断，最后一直等用户说完）。排除：回声消除缺参考信号（重启后 4 ch）；旧回答迟到通知误杀（按回答编号过滤）。待证：不带编号的 `response.cancel` 取消了新回答。已补诊断记录，待复现 |
| 1c | 她答应了却不调用工具（光说不做） | 在真实构建上：一次操作没找到目标后，用户补充名字，她真的发起操作；用户说「算了」时不操作 | 原因已由对照实验确定并修复，待真实构建验收。另有约 2 成首次请求也光说不做，属模型自身问题，未修 |
| 1d | 她听到自己的声音、自言自语 | 用户在 Mac 扬声器上连续聊 5 句：识别出的「用户的话」里没有她自己上一句的原话，她不被自己打断 | 08:07、08:10 两次会话复现（见下方决定），原因未定，停在「戴耳机对照」这一步 |
| 2 | 跑验收任务 1–9 | 每句至少一次真实运行，用户判定并写下原话；证据在 `out/exam/2026-09-25-baseline/`（本地，未入库） | 未开始 |
| 3 | 失败归类 | 用户的原话被归成若干类，每类有例子；用户认可这个分类 | 未开始 |

## 已做的决定

| 日期 | 决定 |
| --- | --- |
| 2026-09-25 | 以本页为下一个目的地（用户同意） |
| 2026-09-25 | 麦克风修复：监听 `AVAudioEngineConfigurationChange`，收到后在新引擎上重建录音和播放；30 秒内最多重建 3 次，超过就停止会话并显示提示，不假装还在听 |
| 2026-09-25 | 第一版在真实构建上失败：两次会话都循环 3 次后停止（每次新引擎重新开回声消除，又被系统停掉）。已排除麦克风权限（tccd `authValue=2`，允许）。Her 之外的 30 行最小程序复现：开回声消除时引擎 0.2–0.5 s 内被停、收不到声音；关回声消除正常；在同一个引擎上再启动一次则持续运行、回声消除仍开着（连续 3 次一致）。改为先重启同一引擎，失败才重建 |
| 2026-09-25 | 07:45 会话（约 3 分钟、16 句，证据 `out/exam/2026-09-25-baseline/session-0745-trace.json`）：没有出现回答中止或卡住，1b 未复现（该构建不含回答编号记录）。新发现：她三次口头答应「我这就点」却没有调用任何工具；「从第一首播放」找不到目标，如实报告未执行；几乎每句回话有 1–2 次播放断档（underrun） |
| 2026-09-25 | 光说不做：在阶跃上用文字重放 07:45 对话尾部（产品原样指令、工具声明和真实工具返回；证据 `out/exam/2026-09-25-baseline/instruction-experiment/`）。原指令下，失败后用户补充歌名时真的调用 1/8；删掉「不盲目重复动作或重新申请预算」后 11/11，且说「算了」时 3/3 不操作、无自发重试。加一条「必须再次调用」的规则反而让首次请求 0/5 调用，未采用。已删除该半句。首次请求在两种指令下都只有约 75–80% 真的调用，另立问题 |
| 2026-09-25 | 播放断档（只读调查结论，未改代码）：每句回话的第 1 次 underrun 大概率是计数假象——首块只有 20 ms 且近乎静音，第二块晚 14–88 ms 到达；播放没有预缓冲。建议开始播放前先攒约 150 ms 并把计数改准；在 60 句探针数据上回放，underrun 由 62 降到 3。第 2 次 underrun 的来源未测。阶段 2 契约中「网络送达不是一卡一卡的原因」依据的是假设的 35 ms 缓冲垫，代码中为 0，修复时在该契约证据节追加更正 |
| 2026-09-25 | 自言自语（08:07、08:10 会话，证据 `out/exam/2026-09-25-baseline/session-0807-0810-self-echo-trace.json`）：识别出的「用户的话」逐字是她上一句的内容（如她说「今天天气不错，要不要出去走走」→ 识别「天气不错，要不要」），她说话时麦克风峰值 −22 到 −33 dBFS（阶段 2 记录的真实插话为 −16 到 −20）。结论：Her 里的回声消除基本失效。已排除：系统音量过大（19%）；外接设备（内置麦克风和扬声器）；同一引擎重启使回声消除失效（最小程序 `echo-experiment/echo-test.swift` 实测：关回声消除时放音期麦克风均值 −40 dBFS，重启后 −60 到 −67，重接连接后 −61 到 −62）；正常与异常会话的初始化次数和引擎个数不同（都是 4–5 次、2 个引擎）。唯一观察到的差别：正常的 07:45 会话里系统发出配置变化但没有停掉引擎，异常会话里引擎被停后由 Her 重启；最小程序无法复现这一差别带来的回声差异。下一步：用户戴耳机对照（不再自言自语 → 回声问题，加「识别内容与她刚说的话几乎相同则丢弃」的保护；仍然乱 → 方向错误，重新查）。在此之前，戴耳机是临时用法 |
| 2026-09-25 | 当日停止，用户决定。本日代码改动（麦克风同一引擎重启、回答编号诊断记录、删去「不盲目重复动作或重新申请预算」）均未提交；「光说不做」修复尚未在真实构建上验收 |

## 停放（这一段不碰）

沿用产品定义地图的停放表，去掉已移到本页的两条（麦克风、验收任务基线）。阶段 4 设计前要定的两件事也仍在那张地图上。

## 收件箱

| 日期 | 是什么 | 可能对哪一步有用 |
| --- | --- | --- |
