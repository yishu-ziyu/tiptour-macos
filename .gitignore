node_modules/
.build/
DerivedData/
.DS_Store
worker/.dev.vars
worker/.wrangler/
*.xcuserstate

# release.sh artifacts — large, regenerated each release, never source
build/
releases/
*.xcarchive/
*.dmg

# Local profiling and JEV experiments
*.profraw
harnesses/jev-loop/*.jsonl

# Local secrets for development probes — never commit
.env
.env.local

# SwiftPM build artifacts for the investigation harness
tools/*/.build/
